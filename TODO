General
  * Client
  * Nested socket iterators.. clients return a "callout" or something
  * defer_to_thread-style behavior

HTTP 
  * Keep-alive doesn't seem to be working
  * Send chunks
  * Http Client
